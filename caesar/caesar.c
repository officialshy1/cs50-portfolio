#include <cs50.h>
#include <stdio.h>
#include <string.h>
#include <ctype.h>
#include <stdlib.h>

int main(int argc, string argv[])
{
    // one command line argument
    if(argc !=2)
    {
        printf("usage: ./caesar key\n");
        return1;
    }
    else printf("Success!\n");

    //key
    string key = argv[i];

    //input is digit
    for (int i )

}